# Archive

One-off scrape probes and the pre-Next static demo dashboard.
Kept for reference; not part of the production path.

- `probes/` — early kaufDA API / DOM experiments (superseded by `scraper/kaufda_json.py`)
- `demo/` — Streamlit/HTML dashboard prototype (superseded by `dashboard/`)
